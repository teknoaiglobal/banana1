
export interface ImageFile {
  name: string;
  type: string;
  size: number;
  base64: string;
  dataUrl: string;
}

export interface UserInfo {
  name: string;
  email: string;
  picture: string;
}
